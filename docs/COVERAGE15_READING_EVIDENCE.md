# 第15批文字读取与年度身份依据

中国联通2025年报原件SHA为 `f13c0ee26f35fa8d3486c0464f372dd6189c6f591e36dc38c3110c89d4638c4b`。PDF物理77–79页的StructParents与2–4页碰撞，ParentTree的ActualText明确指向其他页面。只有具备这条结构链、完整年度与合并列证据的财务页可使用PyMuPDF原生字形读取。选择不依据金额能否勾稽，不按公司代码硬编码。默认原文保留；记录PDF SHA、页码、两份全文/哈希、结构引用、提取器版本。最多6页、每份页面文字16000字符、累计96000字符，并计入原有总文字上限。未采用全局忽略ActualText或OCR。

国航2025年报封面为图像。身份依据来自报告期定义、法定名称及A股代码、独立完整年度财务报表封面、同主体/同年/有序比较期三表，保留准确字符区间、原页SHA及PDF SHA。实际存在的CAS审计文字作为额外证据；不从图片捏造文字。错年、半年、摘要、错公司、错指纹、歧义多表均拒绝。年度身份不代替金额检查：物理79页无原生文字，仍保留needs_review。

复星医药现金流物理152–153页的独立附注列64与两期金额分别读取；白云山物理165页保留“五、（六十四）”及单位“人民/币元”的原始换行。限定完整附注表头、一个附注、两个金融数值；混合重复附注、额外数值、空白、未知科目或母公司边界不得被跳过。亏损标签只认原文同义标签，括号负数保留。

证据在快照、界面、HTML和人工复核JSON中保留；研究案件8KB摘要只保存经验证的指纹及完整底稿引用，不扩大容量。五项人工复核门禁、64KB底稿与既有资源限制保持。新增反例测试和旧132份回归共同防止串列、借值及历史结果漂移。线上是否已部署另见对应发布回执。
